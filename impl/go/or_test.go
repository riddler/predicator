
	package predicator

	import (
		"testing"
	)

	// Test_or -- AUTOGENERATED DO NOT EDIT
	func Test_or(t *testing.T) {
		instructions := [][]interface {}{[]interface {}{"lit", false}, []interface {}{"jtrue", 2}, []interface {}{"lit", true}}
		tt := []struct{
			Name string
			Result bool
			Data   map[string]interface{} 
		}{
		
			{
				"with_no_context",
				true,
				map[string]interface {}(nil),
			},
		
		}
		for _, test := range tt {
			e := NewEvaluator(instructions, test.Data)
			got := e.result()
			if  got != test.Result {
				t.Logf("FAILED %s_%s expected %v got %v", "or", test.Name, test.Result, got)
				t.Fail()
			}
			if e.stack.count > 0 {
				t.Logf("FAILED %s_%s expected empty stack",  "or", test.Name)
				t.Fail()
			}
		}
	}
	